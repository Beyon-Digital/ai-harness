agent_spec_id: fixture-parent
version: "1"
agent_loop:
  id: fixture-loop
  version: "1"
runtime_profile: local-trusted
instructions: "Deterministic fixture behavior is provided by test script input."
requested_capabilities:
  agent:
    spawn:
      max_children: 4
  workspace:
    - read
    - fork
    - merge
resource_budget:
  child_run_slots: 4
  wall_clock_ms: 60000
metadata:
  purpose: integration-test
