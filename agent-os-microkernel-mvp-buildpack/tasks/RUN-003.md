# RUN-003 — Implement dependency satisfaction and atomic ready-run claiming

**Phase:** `runtime`  
**Depends on:** [RUN-002](RUN-002.md)

## Goal

Provide deterministic readiness semantics and single-winner run claims.

## Required outputs / expected code locations

- `crates/run-graph/src/readiness.rs`
- `crates/runtime/src/claim.rs`

## Normative inputs

- Use the component specs linked from the goal/steps and `DECISIONS.md`.

## Implementation steps

1. Implement `completed_successfully`, `any_terminal`, `completed_or_cancelled`.
2. Evaluate dependencies inside the claim transaction.
3. CAS Ready/unclaimed -> Running/claimed with claim token/owner/expiry.
4. Reject claim when cancellation/recovery blocks execution.
5. Expired claim can be reclaimed only under recovery policy/fence.

## Required tests

- `all dependency condition tests`
- `100-concurrent-claim single winner`
- `claim blocked by recovery disposition test`

## Acceptance criteria

- [ ] Exactly one authoritative claim exists for a runnable run.

## Forbidden shortcuts

- No task-specific additions beyond `AGENTS.md` prohibited shortcuts.

## Completion evidence

Record in `TASK_STATUS.yaml`:

- exact test commands executed;
- pass/fail result;
- notable concurrency/fault-injection seed if relevant;
- git commit SHA if available;
- any non-blocking follow-up explicitly outside this task.
