# RUN-001 — Implement AgentRun state machine and revision rules

**Phase:** `runtime`  
**Depends on:** [CMD-001](CMD-001.md), [FND-003](FND-003.md), [RUN-000](RUN-000.md)

## Goal

Implement pure transition validation plus transaction-backed run mutations.

## Required outputs / expected code locations

- `crates/runtime/src/state.rs`
- `crates/runtime/src/run.rs`

## Normative inputs

- Use the component specs linked from the goal/steps and `DECISIONS.md`.

## Implementation steps

1. Encode exact allowed transition matrix from spec.
2. Separate RecoveryDisposition.
3. Every authoritative run mutation increments `run_revision`.
4. Terminal states reject further normal transitions.
5. Store terminal reason codes.

## Required tests

- `table-driven allowed/forbidden transition tests`
- `proptest arbitrary transition sequence`
- `revision increment tests`

## Acceptance criteria

- [ ] Illegal transitions cannot be persisted through runtime APIs.

## Forbidden shortcuts

- No task-specific additions beyond `AGENTS.md` prohibited shortcuts.

## Completion evidence

Record in `TASK_STATUS.yaml`:

- exact test commands executed;
- pass/fail result;
- notable concurrency/fault-injection seed if relevant;
- git commit SHA if available;
- any non-blocking follow-up explicitly outside this task.
