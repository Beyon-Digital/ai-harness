# ADP-004 — Implement port capability negotiation and deterministic resolver

**Phase:** `adapters`  
**Depends on:** [ADP-003](ADP-003.md)

## Goal

Resolve requested port semantics against installed adapters without vendor-name branching.

## Required outputs / expected code locations

- `crates/adapter-registry/src/capabilities.rs`
- `crates/adapter-registry/src/resolver.rs`

## Normative inputs

- Use the component specs linked from the goal/steps and `DECISIONS.md`.

## Implementation steps

1. Load canonical port/version/scope catalog.
2. Match exact compatible major version.
3. Check required adapter capabilities and trust policy.
4. Implement deterministic configured priority/tie-break.
5. Return resolved capabilities for persistence in run environment.
6. T2 sandbox requirement must reject T0 adapter.

## Required tests

- `capability match/miss tests`
- `deterministic tie-break test`
- `sandbox T2 fail-closed test`
- `port version mismatch`

## Acceptance criteria

- [ ] Resolver returns immutable adapter identity + negotiated capability document.

## Forbidden shortcuts

- No task-specific additions beyond `AGENTS.md` prohibited shortcuts.

## Completion evidence

Record in `TASK_STATUS.yaml`:

- exact test commands executed;
- pass/fail result;
- notable concurrency/fault-injection seed if relevant;
- git commit SHA if available;
- any non-blocking follow-up explicitly outside this task.
