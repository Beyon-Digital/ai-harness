# RES-001 — Implement durable resource reservations and budget delegation

**Phase:** `resources`  
**Depends on:** [CMD-001](CMD-001.md), [RUN-001](RUN-001.md)

## Goal

Persist reservations and enforce child budget subset constraints.

## Required outputs / expected code locations

- `crates/resources/src/lib.rs`
- `crates/kernel-store-sqlite/src/repos/resources.rs`

## Normative inputs

- Use the component specs linked from the goal/steps and `DECISIONS.md`.

## Implementation steps

1. Implement reservation lifecycle and typed resource units.
2. Reserve/release in same transaction as run state when correctness requires.
3. Support parent reservation linkage.
4. Compute active descendant allocation totals transactionally before child reservation.
5. Use fencing token for externally allocated resource ownership.

## Required tests

- `over-delegation rejected`
- `release/re-reserve test`
- `concurrent children cannot exceed parent`
- `unknown external allocation recovery test`

## Acceptance criteria

- [ ] Budgets cannot be exceeded by racing child reservations.

## Forbidden shortcuts

- No task-specific additions beyond `AGENTS.md` prohibited shortcuts.

## Completion evidence

Record in `TASK_STATUS.yaml`:

- exact test commands executed;
- pass/fail result;
- notable concurrency/fault-injection seed if relevant;
- git commit SHA if available;
- any non-blocking follow-up explicitly outside this task.
