# EVT-001 — Implement event envelopes, stream keys, cursors, and sequence helpers

**Phase:** `events`  
**Depends on:** [FND-003](FND-003.md), [PST-005](PST-005.md)

## Goal

Create validated durable event primitives and canonical stream-key/cursor encoding.

## Required outputs / expected code locations

- `crates/events/src/envelope.rs`
- `crates/events/src/stream.rs`
- `crates/events/src/cursor.rs`

## Normative inputs

- Use the component specs linked from the goal/steps and `DECISIONS.md`.

## Implementation steps

1. Implement canonical stream key constructors.
2. Cursor contains stream key + last delivered sequence and is opaque at API boundary.
3. Validate sensitivity/retention.
4. Provide event builder that requires event type/version and correlation/causation IDs.

## Required tests

- `stream key round-trip tests`
- `cursor malformed input tests`
- `event envelope protobuf round-trip`

## Acceptance criteria

- [ ] No durable event can exist without event_id, stream_key, sequence, classification.

## Forbidden shortcuts

- No task-specific additions beyond `AGENTS.md` prohibited shortcuts.

## Completion evidence

Record in `TASK_STATUS.yaml`:

- exact test commands executed;
- pass/fail result;
- notable concurrency/fault-injection seed if relevant;
- git commit SHA if available;
- any non-blocking follow-up explicitly outside this task.
