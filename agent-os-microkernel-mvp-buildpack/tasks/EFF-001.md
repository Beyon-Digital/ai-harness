# EFF-001 — Implement effect contract model and conservative policy resolver

**Phase:** `effects`  
**Depends on:** [FND-003](FND-003.md), [FND-004](FND-004.md)

## Goal

Compute the kernel-effective side-effect policy from port semantics, extension claims, trust, conformance, and policy.

## Required outputs / expected code locations

- `crates/effects/src/contract.rs`
- `crates/effects/src/policy.rs`

## Normative inputs

- Use the component specs linked from the goal/steps and `DECISIONS.md`.

## Implementation steps

1. Represent effect class/idempotency/reconciliation/cancellation independently.
2. Create resolver inputs for kernel-known semantics, claim, trust state, conformance evidence, explicit policy.
3. Define conservative merge: unknown/untrusted claims cannot improve safety classification; stronger safe semantics require trusted evidence.
4. Generated/unknown tool default is Opaque/Unknown/Unknown.

## Required tests

- `truth-table policy tests`
- `untrusted self-declared read-only cannot self-promote`
- `trusted built-in known read-only remains read-only`

## Acceptance criteria

- [ ] Resolver never returns safer retry/reconciliation semantics than evidence supports.

## Forbidden shortcuts

- No task-specific additions beyond `AGENTS.md` prohibited shortcuts.

## Completion evidence

Record in `TASK_STATUS.yaml`:

- exact test commands executed;
- pass/fail result;
- notable concurrency/fault-injection seed if relevant;
- git commit SHA if available;
- any non-blocking follow-up explicitly outside this task.
