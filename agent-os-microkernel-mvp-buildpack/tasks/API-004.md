# API-004 — Build local control CLI

**Phase:** `api`  
**Depends on:** [API-002](API-002.md), [API-003](API-003.md)

## Goal

Provide a deterministic operator/developer client used by integration tests and manual inspection.

## Required outputs / expected code locations

- `crates/agentctl/src/main.rs`

## Normative inputs

- Use the component specs linked from the goal/steps and `DECISIONS.md`.

## Implementation steps

1. Commands: health, create-session, put-agent-spec, create-run, get-run, graph, cancel-run, get-effect, resolve-effect, adapters, config show/propose/test/activate, approvals list/respond, events tail/read.
2. Connect only through Control/Event APIs; no DB access.
3. Support JSON output for automation.

## Required tests

- `CLI smoke tests against temp daemon`
- `JSON output parse tests`

## Acceptance criteria

- [ ] Integration tests can drive the full system without internal test-only DB mutation.

## Forbidden shortcuts

- No task-specific additions beyond `AGENTS.md` prohibited shortcuts.

## Completion evidence

Record in `TASK_STATUS.yaml`:

- exact test commands executed;
- pass/fail result;
- notable concurrency/fault-injection seed if relevant;
- git commit SHA if available;
- any non-blocking follow-up explicitly outside this task.
