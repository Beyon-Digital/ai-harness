# INT-004 — Run concurrency and property invariant suite

**Phase:** `integration`  
**Depends on:** [RUN-004](RUN-004.md), [EFF-004](EFF-004.md), [RES-001](RES-001.md), [SCH-001](SCH-001.md), [WRK-003](WRK-003.md), [CFG-002](CFG-002.md)

## Goal

Stress every kernel linearization point under randomized/concurrent schedules.

## Required outputs / expected code locations

- `tests/concurrency/*.rs`
- `tests/property/*.rs`

## Normative inputs

- Use the component specs linked from the goal/steps and `DECISIONS.md`.

## Implementation steps

1. Implement properties from testing/property-tests.md.
2. Use barriers rather than timing sleeps for key race tests.
3. Run hundreds/thousands of iterations where affordable.
4. Persist failure seeds for reproducibility.

## Required tests

- `cargo test --test concurrency`
- `cargo test --test property`

## Acceptance criteria

- [ ] No graph cycle, double effect commit, double timer winner, budget amplification, capability amplification, or double exclusive lease can be produced.

## Forbidden shortcuts

- No task-specific additions beyond `AGENTS.md` prohibited shortcuts.

## Completion evidence

Record in `TASK_STATUS.yaml`:

- exact test commands executed;
- pass/fail result;
- notable concurrency/fault-injection seed if relevant;
- git commit SHA if available;
- any non-blocking follow-up explicitly outside this task.
