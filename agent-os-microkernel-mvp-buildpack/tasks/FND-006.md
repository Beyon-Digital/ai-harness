# FND-006 — Initialize classified tracing and metrics substrate

**Phase:** `foundation`  
**Depends on:** [FND-001](FND-001.md), [FND-003](FND-003.md), [FND-004](FND-004.md)

## Goal

Create observability helpers that attach kernel correlation fields while preventing raw secret payload logging.

## Required outputs / expected code locations

- `crates/observability/src/lib.rs`
- `crates/observability/src/classification.rs`

## Normative inputs

- Use the component specs linked from the goal/steps and `DECISIONS.md`.

## Implementation steps

1. Initialize `tracing_subscriber` in agentd.
2. Provide span helpers for command/run/effect/adapter contexts.
3. Implement classified field wrapper that refuses Secret payload export by default.
4. Define metric names from `specs/observability.md`; wire simple counters/gauges interface, not a remote exporter.

## Required tests

- `classification redaction tests`
- `span creation smoke tests`

## Acceptance criteria

- [ ] Kernel logs include IDs needed for diagnosis.
- [ ] No secret value type implements accidental Display/Debug exposing contents.

## Forbidden shortcuts

- No task-specific additions beyond `AGENTS.md` prohibited shortcuts.

## Completion evidence

Record in `TASK_STATUS.yaml`:

- exact test commands executed;
- pass/fail result;
- notable concurrency/fault-injection seed if relevant;
- git commit SHA if available;
- any non-blocking follow-up explicitly outside this task.
