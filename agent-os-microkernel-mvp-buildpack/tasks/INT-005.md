# INT-005 — Run security and protocol invariant suite

**Phase:** `integration`  
**Depends on:** [SEC-004](SEC-004.md), [SBOX-001](SBOX-001.md), [ADP-005](ADP-005.md), [API-003](API-003.md)

## Goal

Prove the MVP fails closed at its stated security boundaries.

## Required outputs / expected code locations

- `tests/security/*.rs`

## Normative inputs

- Use the component specs linked from the goal/steps and `DECISIONS.md`.

## Implementation steps

1. Forged adapter digest/nonce/protocol rejected.
2. T2 request cannot fall back to T0.
3. Secret not visible in logs/debug/error/event output.
4. Approval changed digest rejected.
5. Delegation confused-deputy case denied.
6. Path traversal/resource URI attacks rejected.
7. World/group-insecure control socket directory rejected or corrected safely.

## Required tests

- `full security integration suite`

## Acceptance criteria

- [ ] Every security claim made by MVP docs has an executable test or is explicitly labeled non-boundary (T0).

## Forbidden shortcuts

- No task-specific additions beyond `AGENTS.md` prohibited shortcuts.

## Completion evidence

Record in `TASK_STATUS.yaml`:

- exact test commands executed;
- pass/fail result;
- notable concurrency/fault-injection seed if relevant;
- git commit SHA if available;
- any non-blocking follow-up explicitly outside this task.
