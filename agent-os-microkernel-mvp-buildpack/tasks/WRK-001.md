# WRK-001 — Implement logical Resource URI parser/resolver

**Phase:** `workspace`  
**Depends on:** [FND-003](FND-003.md), [SEC-002](SEC-002.md)

## Goal

Provide typed logical resource references and capability-checked resolver dispatch.

## Required outputs / expected code locations

- `crates/resource-uri/src/lib.rs`
- `crates/resource-uri/src/parser.rs`
- `crates/resource-uri/src/resolver.rs`

## Normative inputs

- Use the component specs linked from the goal/steps and `DECISIONS.md`.

## Implementation steps

1. Implement canonical schemes.
2. Normalize relative workspace paths and reject traversal.
3. Resolver accepts ExecutionContext and checks permission before adapter-native resolution.
4. Never expose physical secret path/value.

## Required tests

- `URI property tests`
- `path traversal corpus`
- `wrong scheme/type test`
- `permission-denied resolution`

## Acceptance criteria

- [ ] No internal domain record needs an absolute workspace/artifact path.

## Forbidden shortcuts

- No task-specific additions beyond `AGENTS.md` prohibited shortcuts.

## Completion evidence

Record in `TASK_STATUS.yaml`:

- exact test commands executed;
- pass/fail result;
- notable concurrency/fault-injection seed if relevant;
- git commit SHA if available;
- any non-blocking follow-up explicitly outside this task.
