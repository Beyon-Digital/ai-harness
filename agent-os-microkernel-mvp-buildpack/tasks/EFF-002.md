# EFF-002 — Implement EffectRecord transitions and preparation

**Phase:** `effects`  
**Depends on:** [EFF-001](EFF-001.md), [CMD-001](CMD-001.md), [RUN-001](RUN-001.md)

## Goal

Persist effect preparation atomically with the owning decision/command and enforce legal state transitions.

## Required outputs / expected code locations

- `crates/effects/src/record.rs`
- `crates/effects/src/coordinator.rs`
- `crates/kernel-store-sqlite/src/repos/effects.rs`

## Normative inputs

- Use the component specs linked from the goal/steps and `DECISIONS.md`.

## Implementation steps

1. Implement legal state transition table.
2. Prepare immutable request bytes/hash and exact adapter identity from frozen run binding.
3. Preparation occurs inside command/loop-decision transaction.
4. Persist decision/run/step linkage and outbox event.
5. State transitions increment update metadata and reject terminal regression.

## Required tests

- `prepare transaction rollback test`
- `duplicate logical effect identity test`
- `illegal state transition tests`

## Acceptance criteria

- [ ] No effect can be dispatched unless a committed Prepared record exists.

## Forbidden shortcuts

- No task-specific additions beyond `AGENTS.md` prohibited shortcuts.

## Completion evidence

Record in `TASK_STATUS.yaml`:

- exact test commands executed;
- pass/fail result;
- notable concurrency/fault-injection seed if relevant;
- git commit SHA if available;
- any non-blocking follow-up explicitly outside this task.
