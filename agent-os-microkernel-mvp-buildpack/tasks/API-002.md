# API-002 — Implement command/query Control API methods

**Phase:** `api`  
**Depends on:** [API-001](API-001.md), [CMD-001](CMD-001.md), [CFG-003](CFG-003.md), [EFF-004](EFF-004.md), [SEC-003](SEC-003.md)

## Goal

Expose typed base commands and read queries needed to drive the MVP.

## Required outputs / expected code locations

- `crates/control-api/src/control_service.rs`
- `proto/control/**`

## Normative inputs

- Use the component specs linked from the goal/steps and `DECISIONS.md`.

## Implementation steps

1. Map SubmitCommand envelope to typed internal command registry.
2. Expose GetRun/GetTask/GetEffect/GetRunGraph/GetActiveConfig/ListAdapters/RespondApproval/Health.
3. Map stable error codes to gRPC status + structured error metadata.
4. Queries use read transactions/services only.
5. State-changing methods call Command Coordinator only.

## Required tests

- `API idempotency replay test`
- `query not found mapping`
- `approval digest mismatch over API`
- `health reports fencing/config/outbox status`

## Acceptance criteria

- [ ] No public state-changing method performs direct repository mutation.

## Forbidden shortcuts

- No task-specific additions beyond `AGENTS.md` prohibited shortcuts.

## Completion evidence

Record in `TASK_STATUS.yaml`:

- exact test commands executed;
- pass/fail result;
- notable concurrency/fault-injection seed if relevant;
- git commit SHA if available;
- any non-blocking follow-up explicitly outside this task.
