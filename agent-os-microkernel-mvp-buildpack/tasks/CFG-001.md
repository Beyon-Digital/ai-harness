# CFG-001 — Implement inception config parser and schema validation

**Phase:** `config`  
**Depends on:** [ADP-004](ADP-004.md), [ADP-006](ADP-006.md), [FND-002](FND-002.md), [QUE-001](QUE-001.md)

## Goal

Load one internally consistent version-1 config model with explicit binding scopes.

## Required outputs / expected code locations

- `crates/config-engine/src/schema.rs`
- `crates/config-engine/src/model.rs`
- `config/default.yaml`

## Normative inputs

- Use the component specs linked from the goal/steps and `DECISIONS.md`.

## Implementation steps

1. Enforce `schema_version: 1`.
2. Separate bootstrap config from runtime generation document.
3. Parse generation-global services and run-scoped profiles.
4. Reject unknown fields in security-sensitive sections.
5. Validate profile inheritance cycles and missing adapter references.

## Required tests

- `valid default config`
- `version 2 rejected`
- `profile cycle test`
- `unknown sensitive field rejected`

## Acceptance criteria

- [ ] KernelStore selection cannot be changed through runtime profile.

## Forbidden shortcuts

- No task-specific additions beyond `AGENTS.md` prohibited shortcuts.

## Completion evidence

Record in `TASK_STATUS.yaml`:

- exact test commands executed;
- pass/fail result;
- notable concurrency/fault-injection seed if relevant;
- git commit SHA if available;
- any non-blocking follow-up explicitly outside this task.
