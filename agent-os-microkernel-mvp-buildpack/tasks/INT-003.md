# INT-003 — Verify effect crash/restart and reconciliation end-to-end

**Phase:** `integration`  
**Depends on:** [INT-001](INT-001.md), [ADP-005](ADP-005.md)

## Goal

Prove the hardest external-side-effect failure paths with real daemon restart and fixture adapter.

## Required outputs / expected code locations

- `tests/e2e_effect_recovery.rs`

## Normative inputs

- Use the component specs linked from the goal/steps and `DECISIONS.md`.

## Implementation steps

1. Execute fixture counter effect normally.
2. Inject daemon abort after adapter side effect but before acknowledgement persistence.
3. Restart daemon, reacquire higher fence, reconcile by same operation ID, verify counter incremented exactly once.
4. Run non-reconcilable fixture mode and verify `Unknown` + blocked run.
5. Resolve Unknown through Control API with explicit command.

## Required tests

- `all described process-level scenarios`

## Acceptance criteria

- [ ] No test permits automatic duplicate mutation after ambiguous dispatch.

## Forbidden shortcuts

- No task-specific additions beyond `AGENTS.md` prohibited shortcuts.

## Completion evidence

Record in `TASK_STATUS.yaml`:

- exact test commands executed;
- pass/fail result;
- notable concurrency/fault-injection seed if relevant;
- git commit SHA if available;
- any non-blocking follow-up explicitly outside this task.
