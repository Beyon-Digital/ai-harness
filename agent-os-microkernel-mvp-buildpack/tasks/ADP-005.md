# ADP-005 — Build fixture external effect adapter

**Phase:** `adapters`  
**Depends on:** [ADP-004](ADP-004.md), [EFF-004](EFF-004.md)

## Goal

Provide a real external process used to prove protocol, effect idempotency, status lookup, crashes, and fencing.

## Required outputs / expected code locations

- `fixtures/effect-adapter/**`

## Normative inputs

- Use the component specs linked from the goal/steps and `DECISIONS.md`.

## Implementation steps

1. Implement one `fixture.increment_counter` operation keyed by operation ID in its own small durable fixture store.
2. Support execute and status lookup.
3. Make duplicate same operation ID return same result without incrementing twice.
4. Add flags to crash before response, delay response, reject stale fencing token.
5. Package with manifest/lock and content digest.

## Required tests

- `normal execution`
- `duplicate operation ID idempotency`
- `crash-after-side-effect then status reconciliation`
- `wrong handshake digest`

## Acceptance criteria

- [ ] Fixture can demonstrate the exact ambiguity the Effect Coordinator is designed to solve.

## Forbidden shortcuts

- No task-specific additions beyond `AGENTS.md` prohibited shortcuts.

## Completion evidence

Record in `TASK_STATUS.yaml`:

- exact test commands executed;
- pass/fail result;
- notable concurrency/fault-injection seed if relevant;
- git commit SHA if available;
- any non-blocking follow-up explicitly outside this task.
