# REL-001 — Implement automated MVP release gate

**Phase:** `release`  
**Depends on:** [INT-002](INT-002.md), [INT-003](INT-003.md), [INT-004](INT-004.md), [INT-005](INT-005.md)

## Goal

Create one command that proves the microkernel/base-control-plane MVP satisfies all exit criteria.

## Required outputs / expected code locations

- `scripts/release_gate.sh`
- `docs/verification-report-template.md`

## Normative inputs

- Use the component specs linked from the goal/steps and `DECISIONS.md`.

## Implementation steps

1. Run fmt/clippy/build/tests.
2. Run contract hash/schema validation.
3. Run unit/store/concurrency/security/process-level e2e suites.
4. Check no TODO markers tagged `BLOCKING_MVP`.
5. Produce machine-readable and Markdown verification summary with git SHA/toolchain/platform.

## Required tests

- `execute release gate on clean checkout`

## Acceptance criteria

- [ ] All criteria in MVP_EXIT_CRITERIA.md are linked to passing test names/evidence.
- [ ] No skipped required test is reported as pass.

## Forbidden shortcuts

- No task-specific additions beyond `AGENTS.md` prohibited shortcuts.

## Completion evidence

Record in `TASK_STATUS.yaml`:

- exact test commands executed;
- pass/fail result;
- notable concurrency/fault-injection seed if relevant;
- git commit SHA if available;
- any non-blocking follow-up explicitly outside this task.
