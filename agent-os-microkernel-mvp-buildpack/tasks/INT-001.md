# INT-001 — Compose agentd startup/shutdown and basic end-to-end run

**Phase:** `integration`  
**Depends on:** [API-004](API-004.md), [LOOP-002](LOOP-002.md), [SCH-001](SCH-001.md), [RES-001](RES-001.md)

## Goal

Wire the composition root and prove a simple fixture run reaches terminal state through real processes/DB/API.

## Required outputs / expected code locations

- `crates/agentd/src/main.rs`
- `crates/agentd/src/bootstrap.rs`
- `tests/e2e_basic.rs`

## Normative inputs

- Use the component specs linked from the goal/steps and `DECISIONS.md`.

## Implementation steps

1. Instantiate local KernelStore, journal, config, registry, supervisors, workers, API.
2. Register fixture loop bundle.
3. Create session/spec/task/run through CLI/API.
4. Persist frozen environment, claim run, issue turn, fixture returns Complete, commit terminal state/events.
5. Implement drain-first shutdown order from lifecycle spec.

## Required tests

- `full process-level e2e basic test`
- `restart after completed run retains audit data`
- `shutdown while idle`

## Acceptance criteria

- [ ] No fixture test reaches into DB to force normal runtime progress.

## Forbidden shortcuts

- No task-specific additions beyond `AGENTS.md` prohibited shortcuts.

## Completion evidence

Record in `TASK_STATUS.yaml`:

- exact test commands executed;
- pass/fail result;
- notable concurrency/fault-injection seed if relevant;
- git commit SHA if available;
- any non-blocking follow-up explicitly outside this task.
