# SEC-004 — Implement Secrets Broker and macOS Keychain store

**Phase:** `security`  
**Depends on:** [SEC-002](SEC-002.md), [SEC-003](SEC-003.md), [FND-006](FND-006.md)

## Goal

Provide safe secret mediation for trusted local adapters without global environment leakage.

## Required outputs / expected code locations

- `crates/secrets/src/lib.rs`
- `crates/secrets/src/broker.rs`
- `crates/secrets/src/keychain.rs`

## Normative inputs

- Use the component specs linked from the goal/steps and `DECISIONS.md`.

## Implementation steps

1. Define SecretStore trait available only to broker.
2. Implement test in-memory backend.
3. Implement macOS Keychain backend using a narrow wrapper; do not shell-interpolate secret values.
4. Broker evaluates permission and target/egress context.
5. Expose SignOrAct abstraction even if only a fixture implementation is provided initially.
6. Raw secret access returns zeroizing secret type and is never Debug/Display.

## Required tests

- `keychain integration test guarded for macOS/test keychain`
- `secret redaction test`
- `unauthorized secret denied`
- `joint egress policy test`

## Acceptance criteria

- [ ] External adapter environment contains only explicitly injected scoped secret material.

## Forbidden shortcuts

- No task-specific additions beyond `AGENTS.md` prohibited shortcuts.

## Completion evidence

Record in `TASK_STATUS.yaml`:

- exact test commands executed;
- pass/fail result;
- notable concurrency/fault-injection seed if relevant;
- git commit SHA if available;
- any non-blocking follow-up explicitly outside this task.
