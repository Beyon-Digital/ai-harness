# INT-002 — Verify hierarchical children and workspace delegation end-to-end

**Phase:** `integration`  
**Depends on:** [INT-001](INT-001.md), [WRK-003](WRK-003.md)

## Goal

Prove parent/child RunGraph and workspace authority semantics with fixture loops.

## Required outputs / expected code locations

- `tests/e2e_children_workspace.rs`

## Normative inputs

- Use the component specs linked from the goal/steps and `DECISIONS.md`.

## Implementation steps

1. Parent starts from Git fixture workspace.
2. Spawn two parallel write children; each receives isolated fork from same base.
3. Children make separate changes and complete.
4. Parent explicitly merges child outputs; test conflict case separately.
5. Test exclusive-write transfer to one child and stale parent lease rejection.
6. Race cancellation with a third child spawn.

## Required tests

- `parallel fork/merge e2e`
- `exclusive transfer e2e`
- `cancel/spawn race e2e`

## Acceptance criteria

- [ ] Parallel children remain useful because forks inherit exact parent scope/base.

## Forbidden shortcuts

- No task-specific additions beyond `AGENTS.md` prohibited shortcuts.

## Completion evidence

Record in `TASK_STATUS.yaml`:

- exact test commands executed;
- pass/fail result;
- notable concurrency/fault-injection seed if relevant;
- git commit SHA if available;
- any non-blocking follow-up explicitly outside this task.
