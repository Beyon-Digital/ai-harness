# Task EVT-000 — Declare events-module dependencies

- Status: complete (awaiting review)
- Agent: agent-evt000
- Spec: agentd-events
- Commit: `fe5ce98` — chore(workspace): declare events module dependencies [EVT-000]
- Branch: feat/agentd-microkernel-mvp

## What changed

Manifest-only change; every added entry resolves from an existing
`[workspace.dependencies]` line and was appended without removing or reordering
existing lines. The root `agent-os/Cargo.toml` was not edited.

| File | Change |
| --- | --- |
| `agent-os/crates/events/Cargo.toml` | append deps `serde`, `serde_yaml`, `tokio`; new `[dev-dependencies]` `kernel-store-sqlite`, `tempfile`, `testkit`, `tokio` (`domain`, `errors`, `kernel-store` stay) |
| `agent-os/crates/event-journal/Cargo.toml` | append `async-trait.workspace = true` (`domain`, `errors`, `events` were already present) |
| `agent-os/crates/event-journal-sqlite/Cargo.toml` | append deps `async-trait`, `sqlx`, `tokio`; new `[dev-dependencies]` `tempfile` (`domain`, `errors`, `event-journal`, `events` already present) |
| `agent-os/crates/agentd/Cargo.toml` | append `errors`, `events`, `kernel-store` (`observability`, `tokio` stay) |
| `agent-os/Cargo.lock` | regenerated by `cargo check --workspace`; +33 lines |

`tokio` features (`sync`, `rt`, `time`; dev `macros`, `rt-multi-thread`) come from
the single workspace definition (which lists `macros`, `rt-multi-thread`, `sync`,
`time`), as required because `.workspace = true` inherits the workspace feature set.
`sqlx` inherits `runtime-tokio`, `sqlite`, `macros`.

## Acceptance criteria

### 1. All four crates resolve their needs; lock updated — MET

`cargo check --workspace --locked` passes on the committed lockfile:

```
== cargo check --workspace ==
    Finished `dev` profile [unoptimized + debuginfo] target(s) in 3m 23s   (full resolution)
    Finished `dev` profile [unoptimized + debuginfo] target(s) in 15.42s  (cached re-run)
exit: 0

== cargo check --workspace --locked ==
    Finished `dev` profile [unoptimized + debuginfo] target(s) in 19.92s
exit: 0
```

Lockfile delta (`git show HEAD -- agent-os/Cargo.lock`):

```
 name = "agentd"
 dependencies = [
+ "errors",
+ "events",
+ "kernel-store",
  "observability",
  "tokio",
 ]
 ...
 name = "event-journal"
 dependencies = [
+ "async-trait",
  "domain",
  "errors",
  "events",
 ]
 ...
 name = "event-journal-sqlite"
 dependencies = [
+ "async-trait",
  "domain",
  "errors",
  "event-journal",
  "events",
+ "sqlx",
+ "tempfile",
+ "tokio",
 ]
 ...
 name = "events"
 dependencies = [
  "domain",
  "errors",
  "kernel-store",
+ "kernel-store-sqlite",
+ "serde",
+ "serde_yaml",
+ "tempfile",
+ "testkit",
+ "tokio",
 ]
```

### 2. G1/G2 — workspace gates and pack validators pass — MET

```
== cargo test --workspace --no-run ==
  Executable unittests src/lib.rs (target/debug/deps/testkit-8316ac835cf71009)
  Executable unittests src/bin/fixture-daemon.rs (target/debug/deps/fixture_daemon-c940fbb483a2e8de)
  Executable tests/daemon_host.rs (target/debug/deps/daemon_host-5750be8ebeb895e3)
  Executable tests/prop_faults.rs (target/debug/deps/prop_faults-1baa843fe4e2f210)
  Executable tests/store_mock.rs (target/debug/deps/store_mock-2150b0410600352e)
  Executable unittests src/lib.rs (target/debug/deps/workspace-561d4c6a0964266a)
exit: 0

== cargo clippy --workspace --all-targets -- -D warnings ==
    Finished `dev` profile [unoptimized + debuginfo] target(s) in 36.28s (full)
    Finished `dev` profile [unoptimized + debuginfo] target(s) in 14.86s (cached)
exit: 0 (no warnings)

== cargo fmt --check ==
clean (exit 0)

== python3 tools/validate_repo.py ==
OK: 269 markdown, 13 canonical ports, no link/schema/catalog errors
exit: 0

== python3 agent-os-microkernel-mvp-buildpack/scripts/validate_buildpack.py ==
BUILD PACK OK: 59 tasks, 111 markdown files, contracts locked
exit: 0

== bash agent-os/scripts/check-contract-mirror.sh ==
... every contract file OK ...
contract mirror check OK
exit: 0
```

### 3. N3 — no contract, schema, or pack file changed — MET

The commit touches only the four manifests and `agent-os/Cargo.lock`; no file under
`contracts/`, `schema/`, or the build pack appears in `git show --stat HEAD`.

### 4. No file outside `files:` changed — MET

```
$ git show --stat --format='%h %s' HEAD
fe5ce98 chore(workspace): declare events module dependencies [EVT-000]

 agent-os/Cargo.lock                             | 33 +++++++++++++++++++++++++
 agent-os/crates/agentd/Cargo.toml               |  3 +++
 agent-os/crates/event-journal-sqlite/Cargo.toml |  6 +++++
 agent-os/crates/event-journal/Cargo.toml        |  1 +
 agent-os/crates/events/Cargo.toml               |  9 +++++++
 5 files changed, 52 insertions(+)
```

## Verification checklist

- [x] From `agent-os/`: `cargo check --workspace && cargo test --workspace --no-run` — exit 0
- [x] `cargo clippy --workspace --all-targets -- -D warnings` — clean, exit 0
- [x] `cargo fmt --check` — exit 0
- [x] `cargo check --workspace --locked` — committed lockfile in sync
- [x] Pack validators and mirror check — all exit 0
- [x] No deferred-work markers introduced

## Concerns

- `.spec/agentd-events/{ledger,tasks}.md` carry spec-flow bookkeeping edits from
  claim/start/review. They are outside this task's lease and were intentionally left
  uncommitted; they are not part of `fe5ce98`.
- `event-journal` and `event-journal-sqlite` already declared `domain`, `errors`
  (and `events`) from earlier workspace scaffolding, so the task's listed additions
  for those names were already satisfied; only the missing entries were appended and
  nothing was removed.
- Feature granularity is workspace-wide: `.workspace = true` cannot trim `tokio` to
  `sync`/`rt`/`time` for normal deps while keeping `macros`/`rt-multi-thread` for
  dev-deps. The single workspace `tokio` entry covers the union, which satisfies both
  requirements without changing the root manifest.
