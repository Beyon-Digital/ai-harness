# Task EVT-G0 — Reconcile the classification vocabulary

- Status: complete
- Agent: agent-evtg0b (prior attempt agent-evtg0 was blocked; lease amended to include the two blocking paths)
- Spec: agentd-events
- Commit: `4ae9488` — `docs(contracts): reconcile event classification vocabulary [EVT-G0]`
- Branch: current (`feat/agentd-microkernel-mvp`)

## What changed

| File | Change |
| --- | --- |
| `agent-os-microkernel-mvp-buildpack/contracts/events/event.proto` | `PRIVATE` -> `CONFIDENTIAL` (3); retention `EPHEMERAL=1, STANDARD=2, AUDIT=3`; `DURABLE` removed |
| `agent-os-microkernel-mvp-buildpack/specs/kernel-store-schema.sql` | **both** retention CHECKs (`outbox_events` and `artifacts`) narrowed `BETWEEN 1 AND 4` -> `BETWEEN 1 AND 3`; sensitivity/retention comment blocks updated in both tables |
| `agent-os/proto/events/event.proto` | mirror refresh: verbatim copy of the edited pack proto |
| `agent-os/schema/kernel_store.sql` | mirror refresh: verbatim copy of the edited pack schema |
| `agent-os/proto/contract-lock.sha256` | refreshed from the regenerated pack `contracts/contract-lock.sha256` (byte-identical) |
| `agent-os/crates/domain/src/security.rs` | `SensitivityClass::Private` -> `Confidential`; `RetentionClass` -> `Ephemeral=1, Standard=2, Audit=3` (`Session` renamed, `Durable` removed); module tests assert the new names and reject `RetentionClass::from_wire(4)` |
| `agent-os/crates/domain/tests/prop_enums.rs` | `SensitivityClass::Private` -> `Confidential`; `RetentionClass::Session` -> `Standard`; `Durable` assert replaced with `assert!(RetentionClass::from_wire(4).is_err())` |
| `agent-os/crates/events/src/outbox.rs` | 2 x `SensitivityClass::Private` -> `Confidential`; 2 x `RetentionClass::Durable` -> `Standard` |
| `agent-os/crates/command-coordinator/tests/coordinator.rs` | 2 x `SensitivityClass::Private` -> `Confidential`; 2 x `RetentionClass::Durable` -> `Standard` |
| `agent-os/crates/kernel-store-sqlite/tests/repos_remaining.rs` | 1 x `RetentionClass::Session` -> `Standard` |
| `agent-os/crates/kernel-store-sqlite/tests/outbox.rs` | no stale classification usage found; unchanged |
| `agent-os/crates/kernel-store-sqlite/tests/props.rs` | no stale classification usage found; unchanged |
| `agent-os-microkernel-mvp-buildpack/contracts/contract-lock.sha256` | regenerated by `--update-lock` |
| `agent-os-microkernel-mvp-buildpack/MANIFEST.json` | regenerated by `--write-manifest` |

Mirrors are byte-identical to the pack copies:

```
$ diff agent-os-microkernel-mvp-buildpack/specs/kernel-store-schema.sql agent-os/schema/kernel_store.sql \
  && diff agent-os-microkernel-mvp-buildpack/contracts/events/event.proto agent-os/proto/events/event.proto \
  && echo MIRRORS VERBATIM
MIRRORS VERBATIM
```

## Acceptance criteria

### 1. R1.5 — proto, domain, catalog, schema use identical names and ranges — MET

`PUBLIC/INTERNAL/CONFIDENTIAL/SECRET` (1-4) and `EPHEMERAL/STANDARD/AUDIT` (1-3)
in the proto, domain mirror, and schema comments; both schema retention CHECKs
read `BETWEEN 1 AND 3`:

```
$ grep -n "CHECK (retention" agent-os/schema/kernel_store.sql agent-os-microkernel-mvp-buildpack/specs/kernel-store-schema.sql
agent-os/schema/kernel_store.sql:363:  retention INTEGER NOT NULL CHECK (retention BETWEEN 1 AND 3),
agent-os/schema/kernel_store.sql:387:  retention INTEGER NOT NULL CHECK (retention BETWEEN 1 AND 3),
agent-os-microkernel-mvp-buildpack/specs/kernel-store-schema.sql:363:  retention INTEGER NOT NULL CHECK (retention BETWEEN 1 AND 3),
agent-os-microkernel-mvp-buildpack/specs/kernel-store-schema.sql:387:  retention INTEGER NOT NULL CHECK (retention BETWEEN 1 AND 3),

$ grep -n "enum Sensitivity\|enum RetentionClass" agent-os/proto/events/event.proto
3:enum Sensitivity { SENSITIVITY_UNSPECIFIED=0; PUBLIC=1; INTERNAL=2; CONFIDENTIAL=3; SECRET=4; }
4:enum RetentionClass { RETENTION_UNSPECIFIED=0; EPHEMERAL=1; STANDARD=2; AUDIT=3; }
```

The event catalog was already correct and was not edited.

### 2. N3/G2 — validators, mirror check, workspace gates — MET

```
$ python3 agent-os-microkernel-mvp-buildpack/scripts/validate_buildpack.py
BUILD PACK OK: 59 tasks, 111 markdown files, contracts locked
exit: 0

$ python3 tools/validate_repo.py
OK: 271 markdown, 13 canonical ports, no link/schema/catalog errors
exit: 0

$ bash agent-os/scripts/check-contract-mirror.sh
... (shasum lines) ...
contract mirror check OK
exit: 0

$ cargo fmt --check                # from agent-os/
exit: 0

$ cargo test --workspace            # from agent-os/
exit: 0   (82 suites report "test result: ok"; 0 occurrences of FAILED; 184 tests passed)

$ cargo clippy --workspace --all-targets -- -D warnings   # from agent-os/
exit: 0
```

### 3. No stale PRIVATE/SESSION/DURABLE reference — MET

```
$ grep -rn "PRIVATE\|DURABLE" agent-os-microkernel-mvp-buildpack/contracts agent-os/proto agent-os/crates \
    --include="*.proto" --include="*.rs"
(no output; exit 1)

$ grep -rn "SensitivityClass::Private\|RetentionClass::Session\|RetentionClass::Durable" \
    agent-os/crates/domain/tests/prop_enums.rs agent-os/crates/domain/src/security.rs \
    agent-os/crates/events/src/outbox.rs agent-os/crates/command-coordinator/tests/coordinator.rs \
    agent-os/crates/kernel-store-sqlite/tests/repos_remaining.rs \
    agent-os/crates/kernel-store-sqlite/tests/outbox.rs agent-os/crates/kernel-store-sqlite/tests/props.rs
(no output; exit 1)

$ grep -rn "retention.*BETWEEN 1 AND 4" agent-os/schema/kernel_store.sql \
    agent-os-microkernel-mvp-buildpack/specs/kernel-store-schema.sql
(no output; exit 1)
```

The only remaining `Session`/`Durable` tokens in the tree are unrelated domain
concepts (`SessionId`, `NewSession`, `Durable daemon fence` comments).

### 4. No file outside `files:` changed — MET

Every file written is in the lease. The lease entry
`agent-os-microkernel-mvp-buildpack/contract-lock.sha256` was resolved to the
real contract lock at `agent-os-microkernel-mvp-buildpack/contracts/contract-lock.sha256`
(the path named in the lease does not exist; the validator writes and reads the
`contracts/` path). `agent-os/crates/kernel-store-sqlite/tests/outbox.rs` and
`props.rs` are leased but required no edits. The only other working-tree writes
were the spec-flow claim/start/review bookkeeping (`.spec/agentd-events/`) and
this report.

## Notes

- The prior attempt's blocker is resolved: the amended lease adds
  `agent-os/crates/domain/tests/prop_enums.rs` and `agent-os/proto/contract-lock.sha256`.
- The prior attempt left `artifacts.retention` at `BETWEEN 1 AND 4`; the amended
  task requires both retention CHECKs to read `BETWEEN 1 AND 3`, and both do.
- Commit: `4ae9488` (12 files, one commit).
