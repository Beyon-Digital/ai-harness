{
  "name": "agent-os-desktop",
  "private": true,
  "version": "0.1.0",
  "description": "Native desktop shell for the Agent OS gateway (Tauri v2)",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "build:embedded": "tauri build --config src-tauri/tauri.embedded.conf.json"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
